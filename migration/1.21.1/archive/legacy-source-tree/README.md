This archive holds the preserved 1.12.2 Java source snapshot that used to live under `src/main/java`.

The NeoForge 1.21.1 build no longer treats that tree as part of the live source layout. Normal feature work should happen under `src/neo/java`.

Use this archive only for forensic comparison when a ported behavior still needs historical reference.
